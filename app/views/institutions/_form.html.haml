= pythy_form_for @institution do |f|

  = f.text_field :display_name do
    %span.help-block
      The full displayable name of the institution.

  = f.text_field :abbreviation

  .controls
    .alert.alert-info#url_part

  = f.text_field :domain do
    %span.help-block
      The e-mail domain used by the institution. New users will
      automatically be paired to this institution if their e-mail
      address matches.
  
  = f.actions do
    = f.submit
