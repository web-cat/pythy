%ul.breadcrumb
  %li
    = link_to @course.department.institution.display_name, @course.department.institution
    %span.divider /
  %li
    = link_to @course.department.name, department_path(@course.department)
    %span.divider /
  %li
    = link_to @course.department_name_and_number, course_path(@course)
    %span.divider /
  %li.active
    (all offerings)
  %li.pull-right
    = link_to 'New...', new_course_course_offering_path(@course)

= render partial: 'course_offerings/index', locals: { course_offerings: @grouped_offerings }
