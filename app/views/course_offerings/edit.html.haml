%ul.breadcrumb
  %li
    = link_to @course_offering.course.institution.display_name, @course_offering.course.institution
    %span.divider /
  %li
    = link_to @course_offering.course.number, course_path(@course_offering.course)
    %span.divider /
  %li.active
    = @course_offering.crn
    (editing)

= render 'form'
